{
  "name": "repomind-ai",
  "version": "1.0.0",
  "private": true,
  "description": "RepoMind AI - Autonomous GitHub Engineering Assistant powered by GitAgent",
  "scripts": {
    "dev:backend": "cd backend && ..\\.venv\\Scripts\\python.exe -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:agents": "cd agent-runner && npm run dev",
    "install:all": "cd backend && python -m venv .venv && .venv\\Scripts\\pip install -r requirements.txt && cd ../frontend && npm install && cd ../agent-runner && npm install",
    "build:frontend": "cd frontend && npm run build",
    "start:backend": "cd backend && .venv\\Scripts\\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
