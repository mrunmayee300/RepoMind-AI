spec_version: "0.1.0"
name: documentation
version: 1.0.0
description: Generates README, onboarding guides, and API documentation

model:
  preferred: "openai:gpt-4o-mini"
  constraints:
    temperature: 0.4
    max_tokens: 8192

tools: [read, write, memory]

runtime:
  max_turns: 25

compliance:
  risk_level: low
