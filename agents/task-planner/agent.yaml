spec_version: "0.1.0"
name: task-planner
version: 1.0.0
description: Creates actionable GitHub issues and sprint tasks

model:
  preferred: "openai:gpt-4o-mini"
  constraints:
    temperature: 0.3
    max_tokens: 8192

tools: [read, memory]

runtime:
  max_turns: 20

compliance:
  risk_level: low
