spec_version: "0.1.0"
name: refactor
version: 1.0.0
description: Suggests cleaner code structures and incremental refactoring

model:
  preferred: "openai:gpt-4o-mini"
  constraints:
    temperature: 0.3
    max_tokens: 8192

tools: [read, memory]

runtime:
  max_turns: 25

compliance:
  risk_level: low
