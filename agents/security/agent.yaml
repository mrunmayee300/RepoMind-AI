spec_version: "0.1.0"
name: security
version: 1.0.0
description: Finds hardcoded secrets, unsafe patterns, and auth weaknesses

model:
  preferred: "openai:gpt-4o-mini"
  constraints:
    temperature: 0.1
    max_tokens: 8192

tools: [read, cli]

runtime:
  max_turns: 25

compliance:
  risk_level: high
